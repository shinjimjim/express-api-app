<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>お問い合わせフォーム</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <!--Bootstrap 5 のCSSをCDNから読み込む（スタイルを美しくする）-->
</head>
<body class="container py-5"> <!--container：Bootstrapで中央にレイアウト。py-5：上下にpadding（余白）を追加-->
  <h1 class="mb-4">お問い合わせ</h1> <!--mb-4：下にマージン（margin-bottom）を追加-->

  <% if (typeof error !== 'undefined' && error) { %> <!--error が存在して値があれば、赤字でメッセージを表示。-->
    <!--<p style="color:red;"><%= error %></p>-->
    <div class="alert alert-danger"><%= error %></div> <!--error が定義されていて中身があるとき、Bootstrapの赤い警告ボックスで表示-->
  <% } %>

  <form action="/submit" method="POST"> <!--action="/submit"：送信先URL（index.jsのPOST /submit に対応）method="POST"：フォーム送信の方式（GETと違ってURLにデータを載せない）-->
    <!--<p>-->
    <div class="mb-3"></div>
      <!--名前：<br>-->
      <label for="name" class="form-label">名前</label>
      <input type="text" name="name" id="name" class="form-control" value="<%= name || '' %>"> <!--name="name"：サーバー側で req.body.name という形で受け取れる。value="<%= name || '' %>"：バリデーションエラーで戻ってきた時、前の入力を保持するため-->
    <!--</p>-->
    </div>
    <div class="mb-3"></div>
    <!--<p>-->
      <!--メッセージ：<br>-->
      <label for="message" class="form-label">メッセージ</label>
      <textarea name="message" id="message" rows="5" class="form-control"><%= message || '' %></textarea> <!--<%= message || '' %>：前回の入力内容を残す。空なら空文字にする。-->
    <!--</p>-->
    </div>
    <button type="submit" class="btn btn-primary">送信</button> <!--btn btn-primary：青い送信ボタン-->
    <a href="/messages" class="btn btn-secondary ms-2">メッセージ一覧を見る</a> <!--btn btn-secondary：グレーの「一覧を見る」ボタン。ms-2：左側にちょっとマージン（margin-start）-->
  </form>
</body>
</html>
