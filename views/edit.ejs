<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>投稿の編集</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5"> <!--class="container py-5"：Bootstrapのクラスで、中央寄せと上下余白（padding）を設定-->
  <h1 class="mb-4">✏️ 投稿の編集</h1>

  <% if (error) { %> <!--エラーがあるときに表示-->
    <div class="alert alert-danger"><%= error %></div> <!--error 変数に値がある（=エラーが発生している）ときだけ、メッセージを表示。-->
  <% } %>

  <form action="/messages/edit/<%= message._id %>" method="POST">
    <div class="mb-3">
      <label for="name" class="form-label">名前</label>
      <input type="text" id="name" name="name" class="form-control" value="<%= message.name %>"> <!--value="<%= message.name %>"：現在の投稿の名前を初期表示。-->
    </div>

    <div class="mb-3">
      <label for="message" class="form-label">メッセージ</label>
      <textarea id="message" name="message" class="form-control" rows="4"><%= message.message %></textarea> <!--<%= message.message %>：既存のメッセージを初期表示。-->
    </div>

    <button type="submit" class="btn btn-primary">保存する</button>
    <a href="/messages" class="btn btn-secondary">キャンセル</a>
  </form>
</body>
</html>
